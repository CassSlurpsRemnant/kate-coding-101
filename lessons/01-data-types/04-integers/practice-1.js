/**
 * Addition Challenge
 * Create a list of 3 variables.
 * Each variable name should be something you would like to buy.
 * Each value should be an int (number). We'll get to decimals later.
 * Example: var guitar = 100;
 *
 * Create a variable named "total"
 * Example: var total = ...;
 *
 *
 * Output the total to the console.
 * Make sure the math is good.
 */

var socks = 50;
var candies = 27;
var chips = 10;
var total = socks + candies + chips;

console.log("the total is:", total);