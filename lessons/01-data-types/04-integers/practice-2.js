/**
 * Subtraction Challenge
 * You have 1000 to spend in a store.
 *
 * Create a list of 3 purchases.
 * They can just be int values (numbers).
 *
 * Create a variable named "total"
 * The total should be myMoney minus the 3 purchases.
 * Example: var total = ...;
 *
 * Output the total to the console.
 */

var myMoney = 1030;
var designerBag = 400;
var pants = 60;
var lollipop = 6;
var moneySpent = designerBag + pants + lollipop;
var total = myMoney - moneySpent;

console.log("I had", myMoney, "dolllars, I spent", moneySpent, "dollars, so now i have", total, "dollars.")

