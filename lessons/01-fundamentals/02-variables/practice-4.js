/**
 * Convert the following variables to camelCase.
 * (This is the one you should always use.)
 */

var nameOne = "Kate Farr";
var nameTwo = "Daniel Farr";

console.log("name1 is " + nameOne);
console.log("name2 is " + nameTwo);